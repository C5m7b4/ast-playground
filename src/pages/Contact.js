import encours from "../encours";

const Contact = () => {
  const router = encours.getRouter();
  console.log("contact router", router);
  return (
    <div>
      <h2>Contact</h2>
    </div>
  );
};

export default Contact;
